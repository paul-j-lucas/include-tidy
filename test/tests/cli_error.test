EXPECTED_EXIT=2

test_include_tidy \
    -Xtidy --config=$TEST_CONFIG \
    -Xtidy --error=always \
    data/hello.c

test_include_tidy \
    -Xtidy --config=$TEST_CONFIG \
    -Xtidy -e always \
    data/hello.c
