To test include-tidy's parsing of include-tidy.toml in $PWD, the files in this
subdirectory are needed.  A subdirectory is needed so include-tidy.toml isn't
present for other tests in the parent directory.
